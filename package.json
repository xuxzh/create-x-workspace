{
  "name": "create-x-workspace",
  "version": "0.0.1",
  "description": "create x workspace",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"ts-node src/main.ts",
    "build":"rm -rf dist && tsc && cp package.json dist",
    "publish": "npm run build && cd dist && npm publish"
  },
  "author": "xu_xz",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
